// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"
	"smarticky/ent/noteconnectionaccount"
	"smarticky/ent/noteconnectionitemmap"
	"smarticky/ent/noteconnectionjob"
	"smarticky/ent/predicate"
	"smarticky/ent/user"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// NoteConnectionAccountQuery is the builder for querying NoteConnectionAccount entities.
type NoteConnectionAccountQuery struct {
	config
	ctx          *QueryContext
	order        []noteconnectionaccount.OrderOption
	inters       []Interceptor
	predicates   []predicate.NoteConnectionAccount
	withUser     *UserQuery
	withItemMaps *NoteConnectionItemMapQuery
	withJobs     *NoteConnectionJobQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the NoteConnectionAccountQuery builder.
func (_q *NoteConnectionAccountQuery) Where(ps ...predicate.NoteConnectionAccount) *NoteConnectionAccountQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *NoteConnectionAccountQuery) Limit(limit int) *NoteConnectionAccountQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *NoteConnectionAccountQuery) Offset(offset int) *NoteConnectionAccountQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *NoteConnectionAccountQuery) Unique(unique bool) *NoteConnectionAccountQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *NoteConnectionAccountQuery) Order(o ...noteconnectionaccount.OrderOption) *NoteConnectionAccountQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryUser chains the current query on the "user" edge.
func (_q *NoteConnectionAccountQuery) QueryUser() *UserQuery {
	query := (&UserClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(noteconnectionaccount.Table, noteconnectionaccount.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, noteconnectionaccount.UserTable, noteconnectionaccount.UserColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryItemMaps chains the current query on the "item_maps" edge.
func (_q *NoteConnectionAccountQuery) QueryItemMaps() *NoteConnectionItemMapQuery {
	query := (&NoteConnectionItemMapClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(noteconnectionaccount.Table, noteconnectionaccount.FieldID, selector),
			sqlgraph.To(noteconnectionitemmap.Table, noteconnectionitemmap.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, noteconnectionaccount.ItemMapsTable, noteconnectionaccount.ItemMapsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryJobs chains the current query on the "jobs" edge.
func (_q *NoteConnectionAccountQuery) QueryJobs() *NoteConnectionJobQuery {
	query := (&NoteConnectionJobClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(noteconnectionaccount.Table, noteconnectionaccount.FieldID, selector),
			sqlgraph.To(noteconnectionjob.Table, noteconnectionjob.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, noteconnectionaccount.JobsTable, noteconnectionaccount.JobsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first NoteConnectionAccount entity from the query.
// Returns a *NotFoundError when no NoteConnectionAccount was found.
func (_q *NoteConnectionAccountQuery) First(ctx context.Context) (*NoteConnectionAccount, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{noteconnectionaccount.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *NoteConnectionAccountQuery) FirstX(ctx context.Context) *NoteConnectionAccount {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first NoteConnectionAccount ID from the query.
// Returns a *NotFoundError when no NoteConnectionAccount ID was found.
func (_q *NoteConnectionAccountQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{noteconnectionaccount.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *NoteConnectionAccountQuery) FirstIDX(ctx context.Context) int {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single NoteConnectionAccount entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one NoteConnectionAccount entity is found.
// Returns a *NotFoundError when no NoteConnectionAccount entities are found.
func (_q *NoteConnectionAccountQuery) Only(ctx context.Context) (*NoteConnectionAccount, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{noteconnectionaccount.Label}
	default:
		return nil, &NotSingularError{noteconnectionaccount.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *NoteConnectionAccountQuery) OnlyX(ctx context.Context) *NoteConnectionAccount {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only NoteConnectionAccount ID in the query.
// Returns a *NotSingularError when more than one NoteConnectionAccount ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *NoteConnectionAccountQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{noteconnectionaccount.Label}
	default:
		err = &NotSingularError{noteconnectionaccount.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *NoteConnectionAccountQuery) OnlyIDX(ctx context.Context) int {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of NoteConnectionAccounts.
func (_q *NoteConnectionAccountQuery) All(ctx context.Context) ([]*NoteConnectionAccount, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*NoteConnectionAccount, *NoteConnectionAccountQuery]()
	return withInterceptors[[]*NoteConnectionAccount](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *NoteConnectionAccountQuery) AllX(ctx context.Context) []*NoteConnectionAccount {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of NoteConnectionAccount IDs.
func (_q *NoteConnectionAccountQuery) IDs(ctx context.Context) (ids []int, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(noteconnectionaccount.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *NoteConnectionAccountQuery) IDsX(ctx context.Context) []int {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *NoteConnectionAccountQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*NoteConnectionAccountQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *NoteConnectionAccountQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *NoteConnectionAccountQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *NoteConnectionAccountQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the NoteConnectionAccountQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *NoteConnectionAccountQuery) Clone() *NoteConnectionAccountQuery {
	if _q == nil {
		return nil
	}
	return &NoteConnectionAccountQuery{
		config:       _q.config,
		ctx:          _q.ctx.Clone(),
		order:        append([]noteconnectionaccount.OrderOption{}, _q.order...),
		inters:       append([]Interceptor{}, _q.inters...),
		predicates:   append([]predicate.NoteConnectionAccount{}, _q.predicates...),
		withUser:     _q.withUser.Clone(),
		withItemMaps: _q.withItemMaps.Clone(),
		withJobs:     _q.withJobs.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithUser tells the query-builder to eager-load the nodes that are connected to
// the "user" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *NoteConnectionAccountQuery) WithUser(opts ...func(*UserQuery)) *NoteConnectionAccountQuery {
	query := (&UserClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withUser = query
	return _q
}

// WithItemMaps tells the query-builder to eager-load the nodes that are connected to
// the "item_maps" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *NoteConnectionAccountQuery) WithItemMaps(opts ...func(*NoteConnectionItemMapQuery)) *NoteConnectionAccountQuery {
	query := (&NoteConnectionItemMapClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withItemMaps = query
	return _q
}

// WithJobs tells the query-builder to eager-load the nodes that are connected to
// the "jobs" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *NoteConnectionAccountQuery) WithJobs(opts ...func(*NoteConnectionJobQuery)) *NoteConnectionAccountQuery {
	query := (&NoteConnectionJobClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withJobs = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.NoteConnectionAccount.Query().
//		GroupBy(noteconnectionaccount.FieldName).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *NoteConnectionAccountQuery) GroupBy(field string, fields ...string) *NoteConnectionAccountGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &NoteConnectionAccountGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = noteconnectionaccount.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//	}
//
//	client.NoteConnectionAccount.Query().
//		Select(noteconnectionaccount.FieldName).
//		Scan(ctx, &v)
func (_q *NoteConnectionAccountQuery) Select(fields ...string) *NoteConnectionAccountSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &NoteConnectionAccountSelect{NoteConnectionAccountQuery: _q}
	sbuild.label = noteconnectionaccount.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a NoteConnectionAccountSelect configured with the given aggregations.
func (_q *NoteConnectionAccountQuery) Aggregate(fns ...AggregateFunc) *NoteConnectionAccountSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *NoteConnectionAccountQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !noteconnectionaccount.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *NoteConnectionAccountQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*NoteConnectionAccount, error) {
	var (
		nodes       = []*NoteConnectionAccount{}
		_spec       = _q.querySpec()
		loadedTypes = [3]bool{
			_q.withUser != nil,
			_q.withItemMaps != nil,
			_q.withJobs != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*NoteConnectionAccount).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &NoteConnectionAccount{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withUser; query != nil {
		if err := _q.loadUser(ctx, query, nodes, nil,
			func(n *NoteConnectionAccount, e *User) { n.Edges.User = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withItemMaps; query != nil {
		if err := _q.loadItemMaps(ctx, query, nodes,
			func(n *NoteConnectionAccount) { n.Edges.ItemMaps = []*NoteConnectionItemMap{} },
			func(n *NoteConnectionAccount, e *NoteConnectionItemMap) {
				n.Edges.ItemMaps = append(n.Edges.ItemMaps, e)
			}); err != nil {
			return nil, err
		}
	}
	if query := _q.withJobs; query != nil {
		if err := _q.loadJobs(ctx, query, nodes,
			func(n *NoteConnectionAccount) { n.Edges.Jobs = []*NoteConnectionJob{} },
			func(n *NoteConnectionAccount, e *NoteConnectionJob) { n.Edges.Jobs = append(n.Edges.Jobs, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *NoteConnectionAccountQuery) loadUser(ctx context.Context, query *UserQuery, nodes []*NoteConnectionAccount, init func(*NoteConnectionAccount), assign func(*NoteConnectionAccount, *User)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*NoteConnectionAccount)
	for i := range nodes {
		fk := nodes[i].UserID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "user_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *NoteConnectionAccountQuery) loadItemMaps(ctx context.Context, query *NoteConnectionItemMapQuery, nodes []*NoteConnectionAccount, init func(*NoteConnectionAccount), assign func(*NoteConnectionAccount, *NoteConnectionItemMap)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*NoteConnectionAccount)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(noteconnectionitemmap.FieldAccountID)
	}
	query.Where(predicate.NoteConnectionItemMap(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(noteconnectionaccount.ItemMapsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.AccountID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "account_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *NoteConnectionAccountQuery) loadJobs(ctx context.Context, query *NoteConnectionJobQuery, nodes []*NoteConnectionAccount, init func(*NoteConnectionAccount), assign func(*NoteConnectionAccount, *NoteConnectionJob)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*NoteConnectionAccount)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(noteconnectionjob.FieldAccountID)
	}
	query.Where(predicate.NoteConnectionJob(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(noteconnectionaccount.JobsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.AccountID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "account_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *NoteConnectionAccountQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *NoteConnectionAccountQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(noteconnectionaccount.Table, noteconnectionaccount.Columns, sqlgraph.NewFieldSpec(noteconnectionaccount.FieldID, field.TypeInt))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, noteconnectionaccount.FieldID)
		for i := range fields {
			if fields[i] != noteconnectionaccount.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withUser != nil {
			_spec.Node.AddColumnOnce(noteconnectionaccount.FieldUserID)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *NoteConnectionAccountQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(noteconnectionaccount.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = noteconnectionaccount.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// NoteConnectionAccountGroupBy is the group-by builder for NoteConnectionAccount entities.
type NoteConnectionAccountGroupBy struct {
	selector
	build *NoteConnectionAccountQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *NoteConnectionAccountGroupBy) Aggregate(fns ...AggregateFunc) *NoteConnectionAccountGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *NoteConnectionAccountGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*NoteConnectionAccountQuery, *NoteConnectionAccountGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *NoteConnectionAccountGroupBy) sqlScan(ctx context.Context, root *NoteConnectionAccountQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// NoteConnectionAccountSelect is the builder for selecting fields of NoteConnectionAccount entities.
type NoteConnectionAccountSelect struct {
	*NoteConnectionAccountQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *NoteConnectionAccountSelect) Aggregate(fns ...AggregateFunc) *NoteConnectionAccountSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *NoteConnectionAccountSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*NoteConnectionAccountQuery, *NoteConnectionAccountSelect](ctx, _s.NoteConnectionAccountQuery, _s, _s.inters, v)
}

func (_s *NoteConnectionAccountSelect) sqlScan(ctx context.Context, root *NoteConnectionAccountQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
