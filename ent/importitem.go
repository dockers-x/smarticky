// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"smarticky/ent/importitem"
	"smarticky/ent/importjob"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
)

// ImportItem is the model entity for the ImportItem schema.
type ImportItem struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// SourceNoteKey holds the value of the "source_note_key" field.
	SourceNoteKey string `json:"source_note_key,omitempty"`
	// NoteID holds the value of the "note_id" field.
	NoteID uuid.UUID `json:"note_id,omitempty"`
	// Title holds the value of the "title" field.
	Title string `json:"title,omitempty"`
	// Status holds the value of the "status" field.
	Status string `json:"status,omitempty"`
	// Message holds the value of the "message" field.
	Message string `json:"message,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ImportItemQuery when eager-loading is set.
	Edges            ImportItemEdges `json:"edges"`
	import_job_items *int
	selectValues     sql.SelectValues
}

// ImportItemEdges holds the relations/edges for other nodes in the graph.
type ImportItemEdges struct {
	// Job holds the value of the job edge.
	Job *ImportJob `json:"job,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// JobOrErr returns the Job value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ImportItemEdges) JobOrErr() (*ImportJob, error) {
	if e.Job != nil {
		return e.Job, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: importjob.Label}
	}
	return nil, &NotLoadedError{edge: "job"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ImportItem) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case importitem.FieldID:
			values[i] = new(sql.NullInt64)
		case importitem.FieldSourceNoteKey, importitem.FieldTitle, importitem.FieldStatus, importitem.FieldMessage:
			values[i] = new(sql.NullString)
		case importitem.FieldNoteID:
			values[i] = new(uuid.UUID)
		case importitem.ForeignKeys[0]: // import_job_items
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ImportItem fields.
func (_m *ImportItem) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case importitem.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case importitem.FieldSourceNoteKey:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field source_note_key", values[i])
			} else if value.Valid {
				_m.SourceNoteKey = value.String
			}
		case importitem.FieldNoteID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field note_id", values[i])
			} else if value != nil {
				_m.NoteID = *value
			}
		case importitem.FieldTitle:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field title", values[i])
			} else if value.Valid {
				_m.Title = value.String
			}
		case importitem.FieldStatus:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field status", values[i])
			} else if value.Valid {
				_m.Status = value.String
			}
		case importitem.FieldMessage:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field message", values[i])
			} else if value.Valid {
				_m.Message = value.String
			}
		case importitem.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field import_job_items", value)
			} else if value.Valid {
				_m.import_job_items = new(int)
				*_m.import_job_items = int(value.Int64)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the ImportItem.
// This includes values selected through modifiers, order, etc.
func (_m *ImportItem) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryJob queries the "job" edge of the ImportItem entity.
func (_m *ImportItem) QueryJob() *ImportJobQuery {
	return NewImportItemClient(_m.config).QueryJob(_m)
}

// Update returns a builder for updating this ImportItem.
// Note that you need to call ImportItem.Unwrap() before calling this method if this ImportItem
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *ImportItem) Update() *ImportItemUpdateOne {
	return NewImportItemClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the ImportItem entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *ImportItem) Unwrap() *ImportItem {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: ImportItem is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *ImportItem) String() string {
	var builder strings.Builder
	builder.WriteString("ImportItem(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("source_note_key=")
	builder.WriteString(_m.SourceNoteKey)
	builder.WriteString(", ")
	builder.WriteString("note_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.NoteID))
	builder.WriteString(", ")
	builder.WriteString("title=")
	builder.WriteString(_m.Title)
	builder.WriteString(", ")
	builder.WriteString("status=")
	builder.WriteString(_m.Status)
	builder.WriteString(", ")
	builder.WriteString("message=")
	builder.WriteString(_m.Message)
	builder.WriteByte(')')
	return builder.String()
}

// ImportItems is a parsable slice of ImportItem.
type ImportItems []*ImportItem
